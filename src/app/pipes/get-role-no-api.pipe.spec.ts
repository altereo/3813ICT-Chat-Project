import { GetRoleNoAPIPipe } from './get-role-no-api.pipe';

describe('GetRoleNoAPIPipe', () => {
  it('create an instance', () => {
    const pipe = new GetRoleNoAPIPipe();
    expect(pipe).toBeTruthy();
  });
});
